node_modules/
dist/
tsconfig.tsbuildinfo
# credentials -- never commit
# `.env` alone does NOT match .env.local, which is where Vite actually looks.
.env
.env.*
!.env.example
.earthdata_token
# source archives (large, supplied separately)
*.zip
*.rar

# local diagnostics
shot*.png
__*.mjs

# Range-fetched GRIB cache from scripts/fetch-ecmwf.py. Resumable scratch, not
# source: the script writes it to $SCRATCH, which defaults to the working
# directory, so a run without SCRATCH set drops 13 MB here.
ec-tp-*.grib2
tp*.grib2
